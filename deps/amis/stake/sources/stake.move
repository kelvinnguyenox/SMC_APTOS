module amnis::router {
    public entry fun deposit_and_stake_entry(depositor: &signer, amount: u64, receiver: address)  {
    }

    public entry fun unstake_entry(user: &signer, amount: u64, receiver: address) {
    }
}
